#ifndef CDC_CMD_READ_H
#define CDC_CMD_READ_H

/**
 * @brief Handles the GET_CONF command.
 * @note Usage: GET_CONF
 * Sends the current configuration over CDC.
 */
void cmd_handle_get_conf(void);

#endif // CDC_CMD_READ_H
